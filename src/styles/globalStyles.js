import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        marginBottom: 50,
        marginHorizontal: 15,
    },
    Header: {
        fontWeight: 'bold',
        color: '#5f6769',
    },
});